config:
    base_url: ${base_url}
    name: 按10页展示，查询第1页，其他字段都默认空
    variables:
        baseUrl: /storeServer
        base_url: ''
teststeps:
-   name: ${base_url}/${baseUrl}/screen/screenPlanList
    request:
        headers:
            Content-Type: application/json
        json:
            brandId: ''
            deviceMac: ''
            pageNum: '1'
            pageSize: 10
            planName: ''
            planStatus: ''
            planTypeId: ' '
            screenId: ''
            startEndTime: []
        method: POST
        url: ${base_url}/${baseUrl}/screen/screenPlanList
    validate:
    -   eq:
        - status_code
        - 200
    -   eq:
        - body.code
        - 200
