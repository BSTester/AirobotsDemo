config:
    base_url: ${base_url}
    name: 获取4种投放状态成功
    variables:
        baseUrl: /storeServer
        base_url: ''
teststeps:
-   name: ${base_url}/${baseUrl}/screen/screenPlanStatusList
    request:
        data: ''
        headers: {}
        method: POST
        url: ${base_url}/${baseUrl}/screen/screenPlanStatusList
    validate:
    -   eq:
        - status_code
        - 200
    -   eq:
        - body.code
        - 200
