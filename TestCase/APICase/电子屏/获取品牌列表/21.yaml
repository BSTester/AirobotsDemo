config:
    base_url: ${base_url}
    name: 获取品牌列表成功
    variables:
        baseUrl: /storeServer
        base_url: ''
teststeps:
-   name: ${base_url}/${baseUrl}/screen/brandInfoList
    request:
        data: ''
        headers: {}
        method: POST
        url: ${base_url}/${baseUrl}/screen/brandInfoList
    validate:
    -   eq:
        - status_code
        - 200
    -   eq:
        - body.code
        - 200
