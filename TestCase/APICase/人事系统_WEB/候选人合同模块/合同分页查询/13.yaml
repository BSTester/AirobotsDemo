config:
    base_url: $base_url
    name: 合同分页查询 - 正常返回
    variables:
        baseUrl: ehr-admin
teststeps:
-   name: $baseUrl/entrant/contract/pageContract
    request:
        headers:
            Authorization: $token
            Content-Type: application/json
        json:
            pageNum: 1
            pageSize: 10
            signStatus: ["4"]
        method: POST
        url: $baseUrl/entrant/contract/pageContract
    validate:
    -   eq:
        - status_code
        - 200
    -   eq:
        - body.code
        - 0
    extract:
        entrantContractCode: body.data.list.0.entrantContractCode

