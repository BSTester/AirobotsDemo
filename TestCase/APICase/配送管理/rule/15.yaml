config:
    base_url: $base_url
    name: 导出配送规则-正常返回
    variables:
        baseUrl: /storeServer/deliveryManagement
teststeps:
-   name: $baseUrl/rule/exportDeliveryRuleListExcel
    request:
        headers:
            Authorization: $token
            Content-Type: application/json
        json:
            filename: 配送规则列表
        method: POST
        url: $baseUrl/rule/exportDeliveryRuleListExcel
    validate:
    -   eq:
        - status_code
        - 200
