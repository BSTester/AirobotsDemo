config:
    base_url: ${base_url}
    name: 门店详情-正常返回
    variables:
        baseUrl: /
        base_url: ''
teststeps:
-   name: ${base_url}/storeServer/store/storeBaseInfo/getStoreInfo
    request:
        headers:
            Content-Type: application/json
            Authorization: ${token}
        json:
            storeId: 26053915
        method: POST
        url: ${base_url}/storeServer/store/storeBaseInfo/getStoreInfo
    validate:
    -   eq:
        - status_code
        - 200
    -   eq:
        - body.code
        - 0
